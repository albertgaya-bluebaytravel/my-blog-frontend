// Create Store
export const state = () => ({})

// Getters
export const getters = {
  authorized(state) {
    return state.auth.loggedIn
  },
}

// Mutations
export const mutations = {}

// Actions
export const actions = {}
